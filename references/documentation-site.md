# Documentation site

`cabbage init` creates `docs/package.json` and `docs/.vuepress/config.ts`.

```bash
cabbage docs install   # pnpm install in docs/
cabbage docs dev       # local preview
cabbage docs build     # production build
```

The VuePress build output is `docs/.vuepress/dist/`.
