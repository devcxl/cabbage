# Linking rules

- Link to the canonical document instead of copying its contents.
- Prefer relative repository links.
- ADR/RFC references should be stable.
- Local links must resolve; `cabbage validate` checks Markdown file links.
- Use anchors for sections only when the target heading is stable.
