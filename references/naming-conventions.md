# Naming conventions

- Change ID: kebab-case, e.g. `add-user-login`.
- ADR: `ADR-0001-short-title.md`.
- RFC: `RFC-0001-short-title.md`.
- Stable current-state docs should not include version suffixes.
- Dates use `YYYY-MM-DD`.
- Never use names such as `final`, `final2`, `latest-copy`.
